## python program to apply linear interpolation between corresponding okLAB color channels. alpha should be preserved

## by Mantacid

## import modules
import colour

## argument handling
## needs base color (ordered list of sRGBA channels), accent color (ordered list of sRGB channels) and a tint factor (float between 0 and 1).
